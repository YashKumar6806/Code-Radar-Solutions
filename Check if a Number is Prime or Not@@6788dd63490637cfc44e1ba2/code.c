#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int n;
    scanf("%d",&n);
    if(for(i=2,i==n;i++){
            if(i%n==0)printf("Prime");
            break;
        }
    )
    else if(n==1) printf("Not Prime");
}